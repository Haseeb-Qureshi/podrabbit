<div class="container">
  <div class="jumbotron">
    <table class="table table-striped">
      <thead>
        <tr>
          <th>Podcast</th>
          <th>Episode</th>
          <th>Status</th>
        </tr>
      </thead>
      <tbody>
        <% @episode.each do |episode| %>
        <tr>
          <td><%= episode.podcast.name %></td>
          <td><%= episode.name %></td>
          <td><%= episode.status %></td>
        </tr>
        <% end %>
      </tbody>
    </table>
    <h2 class="btn btn-info">Upload new episode</h2>
  </div>
</div>
